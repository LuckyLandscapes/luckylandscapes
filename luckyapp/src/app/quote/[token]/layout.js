export const metadata = {
  title: 'Your Estimate — Lucky Landscapes',
  description: 'Review your Lucky Landscapes estimate, accept it to schedule, or send us changes.',
};

export default function QuoteLayout({ children }) {
  return children;
}
