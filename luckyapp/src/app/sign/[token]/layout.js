export const metadata = {
  title: 'Sign Service Agreement — Lucky Landscapes',
  description: 'Review and sign your Lucky Landscapes service agreement.',
};

export default function SignLayout({ children }) {
  return children;
}
