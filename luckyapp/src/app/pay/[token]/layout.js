export const metadata = {
  title: 'Pay Invoice — Lucky Landscapes',
  description: 'Securely pay your Lucky Landscapes invoice online.',
};

export default function PayLayout({ children }) {
  return children;
}
